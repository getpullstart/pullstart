---
phase: 01-contract-model-and-mvp-slice
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - README.md
  - PROJECT.md
  - REQUIREMENTS.md
  - ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CNTR-01
  - CNTR-03
must_haves:
  truths:
    - Public docs and planning docs describe the same MVP contract surface and the same narrow proof claim.
    - The repository clearly distinguishes Phase 1 contract work from Phase 2 planner implementation.
    - Deferred platform ideas remain documented honestly without leaking into the MVP promise.
  artifacts:
    - path: README.md
      provides: Public-facing product summary aligned to the finalized contract and proof slice
      contains: setup.spec.yaml
    - path: PROJECT.md
      provides: Product thesis and scope boundary aligned to the finalized Phase 1 outputs
      contains: First proof scenario
    - path: .planning/ROADMAP.md
      provides: Execution-facing phase order and Phase 2 build target aligned to the finalized contract
      contains: Phase 2
  key_links:
    - from: README.md
      to: .planning/PROJECT.md
      via: Both describe the same source-of-truth rule and MVP wedge
      pattern: "setup\\.spec\\.yaml|Clone a repo\\. Let the agent get it running\\."
    - from: PROJECT.md
      to: .planning/ROADMAP.md
      via: Product framing matches the execution roadmap for Phase 2 and later phases
      pattern: "Phase 2|planner|proof repo|scope"
    - from: REQUIREMENTS.md
      to: .planning/REQUIREMENTS.md
      via: Public and execution requirement language agree on the contract boundary
      pattern: "canonical onboarding contract|setup\\.spec\\.yaml|proof repo"
---

<objective>
Align all public-facing and execution-facing documentation to the finalized contract and proof boundary so Phase 2 starts from one coherent story.

Purpose: Implement D-01, D-02, D-04, D-05, and D-06 by reconciling the public docs and planning docs after the schema and proof-slice artifacts are settled.
Output: Public docs and planning docs that say the same thing about what Pullstart is, what it is not, and what Phase 2 should build next.
</objective>

<execution_context>
@/Users/bilalimamoglu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bilalimamoglu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-contract-model-and-mvp-slice/01-01-PLAN.md
@.planning/phases/01-contract-model-and-mvp-slice/01-02-PLAN.md
@README.md
@PROJECT.md
@REQUIREMENTS.md
@ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align public product docs to the finalized MVP slice</name>
  <files>README.md, PROJECT.md</files>
  <action>After 01-01 and 01-02 land, update `README.md` and `PROJECT.md` so they reflect the finalized contract shape and proof boundary. Keep `setup.spec.yaml` as the only source of truth per D-01, keep supporting docs secondary per D-02, and keep the proof claim locked to the first Node.js API + PostgreSQL slice per D-04. Remove or reword any passages that imply runtime implementation already exists or that broader portability has been earned.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
for path in ["README.md", "PROJECT.md"]:
    text = Path(path).read_text()
    for needle in ["setup.spec.yaml", "PostgreSQL"]:
        assert needle in text, (path, needle)
print("public-docs-ok")
PY</automated>
  </verify>
  <done>The public docs now describe the same MVP contract, proof repo, and scope restraint that the Phase 1 artifacts define.</done>
</task>

<task type="auto">
  <name>Task 2: Align planning docs to the same contract and Phase 2 target</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <action>Update `.planning/PROJECT.md` and `.planning/ROADMAP.md` so the execution-facing docs explicitly match the finalized Phase 1 outputs. Preserve the existing roadmap order, but sharpen the Phase 2 description to consume the frozen contract and proof checklist rather than inventing new scope. Keep D-05 visible: Phase 1 ends at documentation-ready contract/proof boundaries, not planner/executor code.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
project = Path(".planning/PROJECT.md").read_text()
roadmap = Path(".planning/ROADMAP.md").read_text()
assert "setup.spec.yaml" in project
assert "Phase 2" in roadmap
assert "Contract Model And MVP Slice" in roadmap
print("planning-docs-ok")
PY</automated>
  </verify>
  <done>The planning docs point Phase 2 at a frozen contract/proof boundary and do not blur Phase 1 with implementation work.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile requirement language across public and planning surfaces</name>
  <files>REQUIREMENTS.md, ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Perform a final consistency pass across `REQUIREMENTS.md`, `ROADMAP.md`, and `.planning/REQUIREMENTS.md` so the contract boundary, proof claim, and deferred-scope language agree with the finalized Phase 1 artifacts. Keep CNTR-01 through CNTR-03 explicit, ensure public requirement framing does not conflict with the stricter schema contract from 01-01, and keep future platform ideas clearly separated per D-06.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
checks = {
    "REQUIREMENTS.md": ["setup.spec.yaml", "proof repo"],
    "ROADMAP.md": ["Phase 1", "Phase 2"],
    ".planning/REQUIREMENTS.md": ["CNTR-01", "CNTR-02", "CNTR-03"],
}
for path, needles in checks.items():
    text = Path(path).read_text()
    for needle in needles:
        assert needle in text, (path, needle)
print("requirements-aligned")
PY</automated>
  </verify>
  <done>Public and planning requirements/roadmap text agree on the MVP source of truth, the proof boundary, and the next implementation phase.</done>
</task>

</tasks>

<verification>
Do one cross-doc read after edits and confirm the repo tells one coherent Phase 1 story from README through roadmap without contradicting the finalized contract/proof artifacts.
</verification>

<success_criteria>
Public docs and planning docs agree on what Pullstart is, what it is not, and what Phase 2 should build, with no conflicting statements about contract truth, scope, or proof status.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-model-and-mvp-slice/01-03-SUMMARY.md`
</output>
