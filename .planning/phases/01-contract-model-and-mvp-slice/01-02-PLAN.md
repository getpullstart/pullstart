---
phase: 01-contract-model-and-mvp-slice
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mvp-proof-scenario.md
  - docs/mvp-architecture.md
  - docs/proof-repo-checklist.md
autonomous: true
requirements:
  - CNTR-02
  - CNTR-03
must_haves:
  truths:
    - The proof repo scenario is specific enough to constrain future implementation decisions.
    - The MVP win condition is one trustworthy runnable path plus one trustworthy blocked outcome, not broad portability.
    - Phase 2 and Phase 3 boundaries are described as consumers of the Phase 1 contract rather than new sources of truth.
  artifacts:
    - path: docs/mvp-proof-scenario.md
      provides: Concrete proof-repo starting states, required evidence, and win conditions
      contains: What counts as a win
    - path: docs/mvp-architecture.md
      provides: Narrow architecture handoff from contract model to planner/executor/verifier
      contains: Layer 2
    - path: docs/proof-repo-checklist.md
      provides: Implementation-ready proof fixture checklist for the first proof repo
      contains: Proof repo checklist
  key_links:
    - from: docs/mvp-proof-scenario.md
      to: docs/proof-repo-checklist.md
      via: Scenario expectations are restated as concrete repo evidence and verification inputs
      pattern: "package\\.json|\\.env\\.example|migration|health"
    - from: docs/mvp-architecture.md
      to: docs/mvp-proof-scenario.md
      via: Planner/executor/verifier layers are explicitly scoped to the proof scenario
      pattern: "proof scenario|health|PostgreSQL|setup\\.spec\\.yaml"
---

<objective>
Define the proof repo boundary tightly enough that later implementation can make concrete choices without inventing extra portability or runtime scope.

Purpose: Implement D-03, D-04, D-05, and D-06 by turning the proof scenario into a specific fixture and success boundary for the first implementation cycle.
Output: A precise proof-repo scenario, an implementation-ready fixture checklist, and an architecture note that points Phase 2 and Phase 3 at the right boundary.
</objective>

<execution_context>
@/Users/bilalimamoglu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bilalimamoglu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contract-model-and-mvp-slice/01-CONTEXT.md
@.planning/phases/01-contract-model-and-mvp-slice/01-RESEARCH.md
@docs/mvp-proof-scenario.md
@docs/mvp-architecture.md
@PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the proof scenario into a bounded MVP target</name>
  <files>docs/mvp-proof-scenario.md</files>
  <action>Rewrite `docs/mvp-proof-scenario.md` so it captures the exact proof target from D-03 and D-04: one Node.js API service with PostgreSQL, `.env.example`, migrations, and a health endpoint. Make the starting states concrete enough to drive later inspection logic, including missing-tool, missing-env, and missing-service cases. Define the win condition and blocked condition as implementation-facing truths, not marketing copy. Do not imply broad repo-family portability or Phase 3 execution details.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path("docs/mvp-proof-scenario.md").read_text()
for needle in ["Node.js API", "PostgreSQL", ".env.example", "migration", "health", "What counts as a win"]:
    assert needle in text, needle
assert "arbitrary repos" not in text.lower()
print("scenario-ok")
PY</automated>
  </verify>
  <done>The proof scenario is concrete enough that a future planner can derive repo evidence targets and success checks from it without inventing additional scope.</done>
</task>

<task type="auto">
  <name>Task 2: Create a proof-repo implementation checklist</name>
  <files>docs/proof-repo-checklist.md</files>
  <action>Create `docs/proof-repo-checklist.md` as a tight handoff artifact for later implementation. Translate the proof scenario into a checklist of expected repo evidence, declared contract content, allowed service assumptions, and the single success verification boundary. Reference D-03 and D-05 directly in the prose so the checklist stays narrow. Include what must exist in the proof repo versus what Pullstart should infer later, and explicitly defer portability extras that are not needed for the first proof.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path("docs/proof-repo-checklist.md").read_text()
for needle in ["Proof repo checklist", "setup.spec.yaml", "package.json", ".env.example", "PostgreSQL", "health check"]:
    assert needle in text, needle
print("checklist-ok")
PY</automated>
  </verify>
  <done>The checklist gives Phase 2 and Phase 5 concrete repo evidence and success-boundary inputs without introducing new runtime features or general-purpose abstractions.</done>
</task>

<task type="auto">
  <name>Task 3: Align MVP architecture notes to the proof slice</name>
  <files>docs/mvp-architecture.md</files>
  <action>Update `docs/mvp-architecture.md` so the layer descriptions reflect the finalized Phase 1 contract/proof boundary. Keep Layer 1 anchored to `setup.spec.yaml` per D-01, and tighten Layers 2 through 4 so they describe what Phase 2 and Phase 3 must consume or produce for the proof repo without previewing implementation internals. Add an explicit statement that architecture notes are explanatory and secondary to the contract itself per D-02.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path("docs/mvp-architecture.md").read_text()
for needle in ["setup.spec.yaml", "Planner", "Executor", "Verifier", "proof repo"]:
    assert needle.lower() in text.lower(), needle
assert "plugin runtime" in text
print("architecture-ok")
PY</automated>
  </verify>
  <done>The architecture note now functions as a narrow implementation boundary description that points later phases at the proof slice without becoming a second contract surface.</done>
</task>

</tasks>

<verification>
Confirm the scenario doc, checklist, and architecture note all describe the same proof repo, the same success boundary, and the same phase separation.
</verification>

<success_criteria>
The proof repo definition is specific enough to drive implementation decisions, Phase 2 inherits a clear contract/proof boundary, and no document broadens Pullstart beyond the first Node.js API + PostgreSQL slice.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-model-and-mvp-slice/01-02-SUMMARY.md`
</output>
