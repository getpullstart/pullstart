---
phase: 01-contract-model-and-mvp-slice
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/setup.spec.schema.json
  - contracts/setup.spec.example.yaml
  - docs/onboarding-contract.md
autonomous: true
requirements:
  - CNTR-01
  - CNTR-02
  - CNTR-03
must_haves:
  truths:
    - setup.spec.yaml is the only MVP source of truth for onboarding intent.
    - The contract declares the minimal proof-repo fields needed for tools, services, env expectations, ordered steps, and verification.
    - The contract surface stays small enough for human review and strict enough for reliable parsing.
  artifacts:
    - path: contracts/setup.spec.schema.json
      provides: Strict JSON Schema 2020-12 contract definition for the MVP
      contains: '"$schema": "https://json-schema.org/draft/2020-12/schema"'
    - path: contracts/setup.spec.example.yaml
      provides: Canonical proof-repo example that conforms to the schema
      contains: "version: 0"
    - path: docs/onboarding-contract.md
      provides: Human-readable contract rules and source-of-truth boundaries
      contains: Canonical surface
  key_links:
    - from: contracts/setup.spec.example.yaml
      to: contracts/setup.spec.schema.json
      via: Example fields match schema-required top-level sections
      pattern: "version|repo|tools|services|env|steps|verify"
    - from: docs/onboarding-contract.md
      to: contracts/setup.spec.schema.json
      via: Documentation names the schema file and supported section set
      pattern: "setup\\.spec\\.schema\\.json|JSON Schema|tools|services|env|steps|verify"
---

<objective>
Freeze the smallest executable MVP contract surface for Pullstart so Phase 2 can parse one stable shape instead of guessing from prose.

Purpose: Implement D-01, D-02, D-05, and D-06 by turning the current framing into a concrete schema, canonical example, and authoring guide.
Output: A strict `setup.spec.yaml` schema, a conforming proof-repo example contract, and a contract guide that explains supported sections and excluded complexity.
</objective>

<execution_context>
@/Users/bilalimamoglu/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bilalimamoglu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-contract-model-and-mvp-slice/01-CONTEXT.md
@.planning/phases/01-contract-model-and-mvp-slice/01-RESEARCH.md
@contracts/setup.spec.example.yaml
@docs/onboarding-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the strict MVP contract schema</name>
  <files>contracts/setup.spec.schema.json</files>
  <action>Create `contracts/setup.spec.schema.json` as the MVP contract authority per D-01. Use JSON Schema Draft 2020-12 with `additionalProperties: false` on every object shape so the contract is reliable for parsing per CNTR-03. Support only the proof-slice sections justified by D-03 and CNTR-02: `version`, `repo`, `tools`, `services`, `env`, `steps`, and `verify`. Keep the shape declarative per D-05: no planner branching language, retry policy DSL, condition trees, or executor-specific control flow. Add field descriptions and at least one example for ambiguous fields so the schema remains reviewable by humans.</action>
  <verify>
    <automated>python3 - <<'PY'
import json, pathlib
schema = json.loads(pathlib.Path("contracts/setup.spec.schema.json").read_text())
assert schema["$schema"] == "https://json-schema.org/draft/2020-12/schema"
required = {"version", "repo", "tools", "services", "env", "steps", "verify"}
assert required.issubset(schema["properties"])
assert set(schema["required"]) == required
print("schema-ok")
PY</automated>
  </verify>
  <done>`contracts/setup.spec.schema.json` exists, rejects undeclared fields, and exposes only the minimal top-level contract surface needed for the Node.js API + PostgreSQL proof path.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the example contract to match the schema exactly</name>
  <files>contracts/setup.spec.example.yaml</files>
  <action>Update `contracts/setup.spec.example.yaml` into the canonical proof-repo fixture per D-01 and D-03. Replace the current looser structure with the finalized schema sections from Task 1. Preserve the real proof flow: Node.js toolchain, PostgreSQL service options, env expectations from `.env.example`, ordered install/migrate/start steps, and one health verification path. Do not hide required behavior inside `fallback_notes`; if an option matters for the proof slice, model it structurally or remove it. Keep comments light and only where they improve reviewability.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
spec = Path("contracts/setup.spec.example.yaml").read_text()
for needle in ["version: 0", "repo:", "tools:", "services:", "env:", "steps:", "verify:"]:
    assert needle in spec, needle
assert "fallback_notes:" not in spec
print("example-ok")
PY</automated>
  </verify>
  <done>The example contract is the single canonical proof-repo spec, contains every required MVP section, and no longer relies on prose-only fallback behavior for required setup intent.</done>
</task>

<task type="auto">
  <name>Task 3: Document contract rules and source-of-truth boundaries</name>
  <files>docs/onboarding-contract.md</files>
  <action>Revise `docs/onboarding-contract.md` so it explicitly implements D-02, D-05, and D-06. Name `setup.spec.yaml` as the only canonical contract surface, reference `contracts/setup.spec.schema.json` as the validation contract, list the supported section set from Task 1, and call out what is intentionally excluded in Phase 1: planner logic, executor mechanics, blocker taxonomy, and broad portability promises. The doc should explain why supporting docs remain secondary without sounding like they are ignored.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path("docs/onboarding-contract.md").read_text()
for needle in ["setup.spec.yaml", "setup.spec.schema.json", "tools", "services", "env", "steps", "verify"]:
    assert needle in text, needle
for forbidden in ["planner implementation", "executor implementation"]:
    assert forbidden not in text.lower()
print("doc-ok")
PY</automated>
  </verify>
  <done>The contract guide names the canonical spec, documents the supported MVP schema, and clearly separates executable contract truth from explanatory docs.</done>
</task>

</tasks>

<verification>
Confirm the schema, example contract, and contract guide describe the same supported field set and the same narrow MVP boundary.
</verification>

<success_criteria>
`setup.spec.yaml` is frozen as the Phase 1 source of truth, the supported schema is minimal and explicit, and the contract documentation removes ambiguity about what Pullstart Phase 2 is allowed to parse.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-model-and-mvp-slice/01-01-SUMMARY.md`
</output>
