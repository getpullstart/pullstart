# Examples

This directory exists to clarify Pullstart's first contract and proof scenario.

It is not meant to simulate a broad ecosystem yet.

The current reference example is [contracts/setup.spec.example.yaml](/Users/bilalimamoglu/repos/pullstart/contracts/setup.spec.example.yaml), which models the first Node.js API proof scenario.
